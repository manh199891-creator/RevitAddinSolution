# ChatGPT Web review

Review `PLAN_REVIEW.json`, `CONTRACT_SEED.json`, `WORK_SPLIT.json`, and both worker results. Authorize integration explicitly only after acceptance criteria, ownership, tests, and protected-path checks pass. Without explicit authorization, integration remains blocked.
