# Antigravity.DrawBeams Documentation

Canonical add-in-specific artifacts live here.

```text
docs/
├── plans/        # active/approved implementation plans
├── design/       # technical design decisions for DrawBeams
├── acceptance/   # feature acceptance criteria
└── reports/      # implementation/closure/review reports
```

Do not use repository-root `PLAN.md` or `.ai-bridge` as the permanent home of DrawBeams work. `.ai-bridge` may mirror the active plan only for execution handoff.
