<Window x:Class="Antigravity.ArchModeling.UI.ArchModelingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:Antigravity.Core.UI.Controls;assembly=Antigravity.Core"
        xmlns:local="clr-namespace:Antigravity.ArchModeling.UI"
        Title="Vilai Viet - Arch Modeling" Height="620" Width="960"
        WindowStartupLocation="CenterScreen" ResizeMode="CanResize"
        Background="#FFFFFF" MouseLeftButtonDown="Window_MouseLeftButtonDown"
        FontFamily="Segoe UI" UseLayoutRounding="True" SnapsToDevicePixels="True" MinWidth="760" MinHeight="480">

        <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Antigravity.Core;component/UI/Themes/DesignTokens.xaml"/>
                <ResourceDictionary Source="pack://application:,,,/Antigravity.Core;component/UI/Themes/Typography.xaml"/>
                <ResourceDictionary Source="pack://application:,,,/Antigravity.Core;component/UI/Themes/Controls.xaml"/>
                <ResourceDictionary Source="pack://application:,,,/Antigravity.Core;component/UI/Themes/DataControls.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        <local:HatchPreviewConverter x:Key="HatchPreviewConverter"/>
        <Style TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Foreground" Value="#000000"/>
        </Style>
        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#4B5563"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
        </Style>
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="Foreground" Value="#000000"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="CaretBrush" Value="#007ACC"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
        <Style x:Key="PrimaryBtn" TargetType="Button">
            <Setter Property="Background" Value="#007ACC"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" CornerRadius="4" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#3399FF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#005C99"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="SecondaryBtn" TargetType="Button">
            <Setter Property="Background" Value="#E5E7EB"/>
            <Setter Property="Foreground" Value="#000000"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" CornerRadius="4" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#E5E7EB"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#D1D5DB"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DarkDataGrid" TargetType="DataGrid">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="Foreground" Value="#000000"/>
            <Setter Property="RowBackground" Value="#FFFFFF"/>
            <Setter Property="AlternatingRowBackground" Value="#F9FAFB"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#E5E7EB"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="CanUserAddRows" Value="False"/>
        </Style>
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#F5F6F8"/>
            <Setter Property="Foreground" Value="#000000"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
        </Style>
        <Style TargetType="DataGridRow">
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#E5F1FB"/>
                    <Setter Property="Foreground" Value="#000000"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="DataGridCell">
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Padding="{TemplateBinding Padding}" Background="Transparent" BorderThickness="0">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Foreground" Value="#000000"/>
                    <Setter Property="BorderBrush" Value="Transparent"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <controls:BrandHeader x:Name="WindowHeader" Grid.Row="0" TitleText="Arch Modeling" SubtitleText="Create elements automatically from AutoCAD entities"/>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250"/>
                <ColumnDefinition Width="15"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="#F9FAFB" BorderBrush="#E5E7EB" BorderThickness="1" CornerRadius="6" Padding="15">
                <StackPanel>
                    <Button x:Name="BtnSetOrigin" Content="🎯  Set Origin" Style="{StaticResource SecondaryBtn}" Margin="0,0,0,15" Click="BtnSetOrigin_Click"/>

                    <TextBlock Text="Base Level" Style="{StaticResource LabelStyle}"/>
                    <ComboBox x:Name="CboBaseLevel" Height="30" Margin="0,0,0,10" DisplayMemberPath="Name"/>

                    <TextBlock Text="Base Offset (mm)" Style="{StaticResource LabelStyle}"/>
                    <TextBox x:Name="TxtBaseOffset" Style="{StaticResource ModernTextBox}" Text="0" Margin="0,0,0,10"/>

                    <TextBlock x:Name="LblTopLevel" Text="Top Level" Style="{StaticResource LabelStyle}"/>
                    <ComboBox x:Name="CboTopLevel" Height="30" Margin="0,0,0,10" DisplayMemberPath="Name"/>

                    <TextBlock x:Name="LblTopOffset" Text="Top Offset (mm)" Style="{StaticResource LabelStyle}"/>
                    <TextBox x:Name="TxtTopOffset" Style="{StaticResource ModernTextBox}" Text="0" Margin="0,0,0,10"/>

                    <CheckBox x:Name="ChkMirrorHinge" Content="🔁 Mirror Hinge"
                              Margin="0,5,0,0" FontSize="12" Foreground="#4B5563"
                              ToolTip="Flip the hinge side for all single-leaf doors"
                              Visibility="Collapsed"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Background="#FFFFFF" BorderBrush="#E5E7EB" BorderThickness="1" CornerRadius="6" Padding="15">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                        <Button x:Name="BtnScanCAD" Content="🔄  Scan CAD Selection" Style="{StaticResource SecondaryBtn}" Click="BtnScanCAD_Click"/>
                        <TextBlock x:Name="TxtStatus" Text="Ready" Margin="15,0,0,0" VerticalAlignment="Center" Foreground="#6B7280"/>
                    </StackPanel>

                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Grid Grid.Column="0" x:Name="GridPrimaryMapping" Grid.ColumnSpan="3">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <Grid Grid.Row="0" Margin="0,0,0,5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock x:Name="TxtWallMappingTitle" Text="WALLS MAPPING" FontWeight="Bold" Foreground="#4B5563" VerticalAlignment="Center" Visibility="Collapsed"/>
                                <Button x:Name="BtnImportWallMapping" Grid.Column="1" Content="📥 Import" Style="{StaticResource SecondaryBtn}" Margin="0,0,4,0" Click="BtnImportWallMapping_Click"/>
                                <Button x:Name="BtnExportWallTemplate" Grid.Column="2" Content="📤 Export" Style="{StaticResource SecondaryBtn}" Click="BtnExportWallTemplate_Click"/>
                            </Grid>
                            <DataGrid x:Name="DgMapping" Grid.Row="1" Style="{StaticResource DarkDataGrid}">
                                <DataGrid.Columns>
                                    <!-- CAD Name col -->
                                    <DataGridTemplateColumn Header="CAD Name" Width="*">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding CadName}" VerticalAlignment="Center" Padding="2,0"/>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>

                                    <!-- Hatch Pattern Preview col (FloorCeil only) -->
                                    <DataGridTemplateColumn x:Name="ColPreview" Header="Pattern" Width="96">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <Image Source="{Binding PatternInfo, Converter={StaticResource HatchPreviewConverter}}"
                                                       Width="80" Height="80"
                                                       Stretch="Uniform" Margin="2"
                                                       RenderOptions.BitmapScalingMode="HighQuality">
                                                    <Image.ToolTip>
                                                        <ToolTip MaxWidth="260">
                                                            <TextBlock Text="{Binding PatternInfo.Description}" TextWrapping="Wrap" FontSize="11"/>
                                                        </ToolTip>
                                                    </Image.ToolTip>
                                                </Image>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                    <DataGridTextColumn x:Name="ColThickness" Header="Width (mm)" Binding="{Binding Thickness}" Width="80" IsReadOnly="True"/>
                                    <DataGridTextColumn Header="Count" Binding="{Binding Count}" Width="60" IsReadOnly="True"/>

                                    <DataGridTemplateColumn x:Name="ColGroup" Header="Group lines" Width="130">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <ComboBox ItemsSource="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=AvailableGroups}"
                                                          SelectedItem="{Binding GroupName, UpdateSourceTrigger=PropertyChanged}"
                                                          Background="#FFFFFF" Foreground="#000000" />
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>

                                    <DataGridTemplateColumn Header="Revit Family" Width="150">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <ComboBox ItemsSource="{Binding AvailableFamilies}"
                                                          SelectedItem="{Binding SelectedFamily, UpdateSourceTrigger=PropertyChanged}"
                                                          Background="#FFFFFF" Foreground="#000000"/>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>

                                    <DataGridTemplateColumn Header="Revit Type" Width="150">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <ComboBox ItemsSource="{Binding FilteredTypes}"
                                                          SelectedItem="{Binding SelectedType, UpdateSourceTrigger=PropertyChanged}"
                                                          DisplayMemberPath="Name"
                                                          IsEditable="True"
                                                          TextSearch.TextPath="Name"
                                                          Background="#FFFFFF" Foreground="#000000">
                                                </ComboBox>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>

                                    <DataGridTextColumn x:Name="ColTypeMark" Header="Type Mark" Binding="{Binding TypeMark, UpdateSourceTrigger=PropertyChanged}" Width="100"/>
                                </DataGrid.Columns>
                            </DataGrid>
                        </Grid>

                        <!-- SECOND GRID FOR DOORS (Visible in Combined Mode) -->
                        <Grid Grid.Column="2" x:Name="GridDoorsMapping" Visibility="Collapsed">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <Grid Grid.Row="0" Margin="0,0,0,5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="DOORS MAPPING" FontWeight="Bold" Foreground="#4B5563" VerticalAlignment="Center"/>
                                <Button x:Name="BtnImportDoorMapping" Grid.Column="1" Content="📥 Import" Style="{StaticResource SecondaryBtn}" Margin="0,0,4,0" Click="BtnImportDoorMapping_Click"/>
                                <Button x:Name="BtnExportDoorTemplate" Grid.Column="2" Content="📤 Export" Style="{StaticResource SecondaryBtn}" Click="BtnExportDoorTemplate_Click"/>
                            </Grid>
                            <DataGrid x:Name="DgMappingDoors" Grid.Row="1" Style="{StaticResource DarkDataGrid}" ItemsSource="{Binding DoorMappingItems}">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="CAD Name" Binding="{Binding CadName}" Width="*" IsReadOnly="True"/>
                                    <DataGridTextColumn Header="Count" Binding="{Binding Count}" Width="60" IsReadOnly="True"/>

                                    <DataGridTemplateColumn Header="Revit Family" Width="120">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <ComboBox ItemsSource="{Binding AvailableFamilies}"
                                                          SelectedItem="{Binding SelectedFamily, UpdateSourceTrigger=PropertyChanged}"
                                                          Background="#FFFFFF" Foreground="#000000"/>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>

                                    <DataGridTemplateColumn Header="Revit Type" Width="120">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <ComboBox ItemsSource="{Binding FilteredTypes}"
                                                          SelectedItem="{Binding SelectedType, UpdateSourceTrigger=PropertyChanged}"
                                                          DisplayMemberPath="Name"
                                                          IsEditable="True"
                                                          TextSearch.TextPath="Name"
                                                          Background="#FFFFFF" Foreground="#000000">
                                                </ComboBox>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>

                                    <DataGridTextColumn Header="Type Mark" Binding="{Binding TypeMark, UpdateSourceTrigger=PropertyChanged}" Width="80"/>
                                </DataGrid.Columns>
                            </DataGrid>
                        </Grid>
                    </Grid>
                </Grid>
            </Border>
        </Grid>

        <Grid Grid.Row="3" Margin="0,15,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Orientation="Horizontal" HorizontalAlignment="Left">
                <Button x:Name="BtnCreate" Content="✅  Create Elements" Style="{StaticResource PrimaryBtn}" Margin="0,0,8,0" Click="BtnCreate_Click"/>
                <Button Content="✖  Cancel" Style="{StaticResource SecondaryBtn}" Click="BtnCancel_Click"/>
            </StackPanel>

        </Grid>
    </Grid>
</Window>


