<Window x:Class="Antigravity.CadVoidPlacer.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:Antigravity.Core.UI.Controls;assembly=Antigravity.Core"
        Title="Vilai Viet Kiểm Tra Lỗ Mở Sàn" Height="620" Width="460"
        WindowStartupLocation="CenterScreen"
        Background="#FFFFFF" FontFamily="Segoe UI" UseLayoutRounding="True" SnapsToDevicePixels="True" ResizeMode="CanResize" MinWidth="420" MinHeight="500">

        <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Antigravity.Core;component/UI/Themes/DesignTokens.xaml"/>
                <ResourceDictionary Source="pack://application:,,,/Antigravity.Core;component/UI/Themes/Typography.xaml"/>
                <ResourceDictionary Source="pack://application:,,,/Antigravity.Core;component/UI/Themes/Controls.xaml"/>
                <ResourceDictionary Source="pack://application:,,,/Antigravity.Core;component/UI/Themes/DataControls.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#000000"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Height" Value="28"/>
            <Setter Property="Padding"      Value="6,2"/>
            <Setter Property="BorderBrush"  Value="#D1D5DB"/>
            <Setter Property="Background"   Value="#FFFFFF"/>
            <Setter Property="Foreground"   Value="#000000"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Name="Border" CornerRadius="4" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="#007ACC"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <!-- Default button (Secondary) -->
        <Style TargetType="Button">
            <Setter Property="Height" Value="32"/>
            <Setter Property="Background" Value="#E5E7EB"/>
            <Setter Property="Foreground" Value="#000000"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" CornerRadius="4" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#D1D5DB"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#9BA3AF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="PrimaryBtn" TargetType="Button">
            <Setter Property="Height" Value="32"/>
            <Setter Property="Background" Value="#007ACC"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" CornerRadius="4" Background="{TemplateBinding Background}" Padding="12,0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#3399FF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#005C99"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="Label">
            <Setter Property="Foreground"  Value="#007ACC"/>
            <Setter Property="FontWeight"  Value="Bold"/>
            <Setter Property="FontSize"    Value="12"/>
            <Setter Property="Padding"     Value="0,0,0,4"/>
        </Style>
    </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#FFFFFF" BorderBrush="#D1D5DB" BorderThickness="0,0,0,1" Margin="-24,-24,-24,16" Padding="16,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Viewbox Grid.Column="0" Width="28" Height="28" Margin="0,0,12,0" VerticalAlignment="Center">
                    <Canvas Width="100" Height="80">
                        <Path Fill="#1B3679" Data="M 30,10 L 70,10 L 50,45 Z"/>
                        <Path Fill="#D8262C" Data="M 10,10 L 32,10 L 50,55 L 68,10 L 90,10 L 50,80 Z"/>
                    </Canvas>
                </Viewbox>
                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock FontSize="14" FontWeight="Bold" Foreground="#111827">
                        <Run Text="VILA" Foreground="#1B3679"/><Run Text="I"><Run.Foreground><LinearGradientBrush StartPoint="0,0" EndPoint="1,0"><GradientStop Color="#1B3679" Offset="0.7"/><GradientStop Color="#D8262C" Offset="0.7"/></LinearGradientBrush></Run.Foreground></Run><Run Text="VIET" Foreground="#D8262C"/><Run Text=" · KIỂM TRA LỖ MỞ SÀN" Foreground="#111827" FontWeight="SemiBold"/>
                    </TextBlock>
                    <TextBlock Text="Tạo các lỗ mở (void) trên sàn tự động từ đường nét AutoCAD" 
                               FontSize="11" Foreground="#6B7280" Margin="0,2,0,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Form -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>

                <!-- ── Step 1: Select geometry ───────────────────────────── -->
                <Label Content="1 · SELECT GEOMETRY IN AUTOCAD"/>
                <TextBlock Text="Choose how to get geometry: Manual selection or Scanning a specific layer."
                           FontSize="11" Foreground="#888888" Margin="0,0,0,8" TextWrapping="Wrap"/>
                
                <TabControl Background="Transparent" BorderThickness="0" Margin="0,0,0,10">
                    <TabItem Header="Manual Pick">
                        <StackPanel Margin="0,10,0,0">
                            <Button x:Name="BtnSelectGeometry" Content="Select Lines / Polylines in AutoCAD"
                                    HorizontalAlignment="Left" Padding="14,7" Margin="0,0,0,6"
                                    Style="{StaticResource PrimaryBtn}" FontSize="12" FontWeight="Bold"
                                    Click="BtnSelectGeometry_Click"/>
                        </StackPanel>
                    </TabItem>
                    <TabItem Header="Scan Layer" x:Name="TabScanLayer" Selector.Selected="TabScanLayer_Selected">
                        <StackPanel Margin="0,10,0,0">
                            <Grid Margin="0,0,0,6">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <ComboBox x:Name="ComboLayers" VerticalContentAlignment="Center" Margin="0,0,8,0"/>
                                <Button Grid.Column="1" Content="Scan Layer" Padding="14,7"
                                        Style="{StaticResource PrimaryBtn}" FontSize="12" FontWeight="Bold"
                                        Click="BtnScanLayer_Click"/>
                            </Grid>
                        </StackPanel>
                    </TabItem>
                </TabControl>

                <TextBlock x:Name="TxtSelStatus"
                           Text="No geometry selected yet."
                           FontSize="11" Foreground="#FF9999" FontStyle="Italic"
                           Margin="0,0,0,20" TextWrapping="Wrap"/>

                <!-- ── Step 2: CAD reference point ───────────────────────── -->
                <Label Content="2 · CAD REFERENCE POINT (mm)"/>
                <TextBlock Text="Pick a known point in AutoCAD (e.g. grid A-1) to align coordinates."
                           FontSize="11" Foreground="#888888" Margin="0,0,0,8" TextWrapping="Wrap"/>
                <Grid Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="16"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="X (mm)" FontSize="11" Foreground="#666666" Margin="0,0,0,4"/>
                        <TextBox x:Name="TxtCadOriginX" Text="0,000"/>
                    </StackPanel>
                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Y (mm)" FontSize="11" Foreground="#666666" Margin="0,0,0,4"/>
                        <TextBox x:Name="TxtCadOriginY" Text="0,000"/>
                    </StackPanel>
                </Grid>
                <Button Content="Pick Reference Point in AutoCAD"
                        HorizontalAlignment="Left" Padding="14,6" Margin="0,0,0,20"
                        FontSize="11" Background="#E5E7EB" Foreground="#111827" BorderThickness="0"
                        Click="BtnPickCadRef_Click"/>

                <!-- ── Step 3: Revit reference point ─────────────────────── -->
                <Label Content="3 · REVIT REFERENCE POINT"/>
                <TextBlock Text="Pick the same reference point inside the Revit model."
                           FontSize="11" Foreground="#888888" Margin="0,0,0,8" TextWrapping="Wrap"/>
                <Button x:Name="BtnPickRevitRef" Content="Pick Reference Point in Revit"
                        HorizontalAlignment="Left" Padding="14,8" Margin="0,0,0,6"
                        Click="BtnPickRevitRef_Click"/>
                <TextBlock x:Name="TxtRevitStatus"
                           Text="No point selected — defaults to (0, 0, 0)"
                           FontSize="11" Foreground="#FF9999" FontStyle="Italic"
                           Margin="0,0,0,20"/>

                <!-- ── Step 4: Select Level ────────────────────────────────── -->
                <Label Content="4 · PLACEMENT LEVEL"/>
                <TextBlock Text="Choose which level the voids will be placed at."
                           FontSize="11" Foreground="#888888" Margin="0,0,0,8" TextWrapping="Wrap"/>
                <ComboBox x:Name="ComboLevels" DisplayMemberPath="Name" Margin="0,0,0,20"
                          VerticalContentAlignment="Center"/>

                <!-- ── Step 5: Void depth ─────────────────────────────────── -->
                <Label Content="5 · VOID DEPTH (mm)"/>
                <TextBlock Text="Extrusion height of the void prism (e.g. 300 for a typical slab)."
                           FontSize="11" Foreground="#888888" Margin="0,0,0,8" TextWrapping="Wrap"/>
                <TextBox x:Name="TxtDepth" Text="300" Margin="0,0,0,4"/>
                <TextBlock Text="Voids are created as DirectShape solids — no Revit family required."
                           FontSize="11" Foreground="#AAAACC" FontStyle="Italic" TextWrapping="Wrap"/>

            </StackPanel>
        </ScrollViewer>

        <!-- Footer -->
        <Grid Grid.Row="2" Margin="0,16,0,0">
            <StackPanel>
                <Separator Background="#E5E7EB" Margin="0,0,0,14"/>
                <Button Content="PLACE VOIDS" Height="46" FontSize="15" FontWeight="Bold"
                        Style="{StaticResource PrimaryBtn}"
                        Click="BtnProcess_Click" Cursor="Hand"/>
            </StackPanel>
            <TextBlock Text="@manhns" Margin="0,0,0,-8" FontSize="12" FontWeight="SemiBold" Foreground="#6B7280" Opacity="1" HorizontalAlignment="Right" VerticalAlignment="Bottom" IsHitTestVisible="False" Focusable="False"/>
        </Grid>
    </Grid>
</Window>



