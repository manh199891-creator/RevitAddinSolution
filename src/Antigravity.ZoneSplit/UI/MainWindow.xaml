<Window x:Class="Antigravity.ZoneSplit.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:Antigravity.Core.UI.Controls;assembly=Antigravity.Core"
        Title="Vilai Viet - Zone Split"
        Width="820" Height="540"
        WindowStartupLocation="CenterScreen"
        Background="#FFFFFF"
        ResizeMode="CanResize"
        Topmost="True" FontFamily="Segoe UI" UseLayoutRounding="True" SnapsToDevicePixels="True" MinWidth="720" MinHeight="480">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Antigravity.Core;component/UI/Themes/DesignTokens.xaml"/>
                <ResourceDictionary Source="pack://application:,,,/Antigravity.Core;component/UI/Themes/Typography.xaml"/>
                <ResourceDictionary Source="pack://application:,,,/Antigravity.Core;component/UI/Themes/Controls.xaml"/>
                <ResourceDictionary Source="pack://application:,,,/Antigravity.Core;component/UI/Themes/DataControls.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        <Style x:Key="PrimaryBtn" TargetType="Button">
            <Setter Property="Height" Value="32"/>
            <Setter Property="Background" Value="#007ACC"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border CornerRadius="4" Background="{TemplateBinding Background}" Padding="12,0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryBtn" TargetType="Button" BasedOn="{StaticResource PrimaryBtn}">
            <Setter Property="Background" Value="#E5E7EB"/>
            <Setter Property="Foreground" Value="#000000"/>
        </Style>

        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Height" Value="28"/>
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="Foreground" Value="#000000"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="6,2"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Name="Border" CornerRadius="4" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="#007ACC"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBoxItem">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="Foreground" Value="#000000"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border Name="Border" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#E5E7EB"/>
                                <Setter Property="Foreground" Value="#000000"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <controls:BrandHeader Grid.Row="0" TitleText="Zone Split" SubtitleText="Calculate quantities from Zone Generic Models"/>

        <Grid x:Name="ZoneWorkspace" Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="5*"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="5*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="{StaticResource VvSurfaceBrush}" BorderBrush="{StaticResource VvBorderBrush}" BorderThickness="1" CornerRadius="4" Padding="16">
                <StackPanel>
                    <TextBlock Style="{StaticResource VvSectionTitleStyle}" Text="ELEMENT CATEGORIES" Margin="0,0,0,12"/>
                    <CheckBox Name="chkAll" Content="Select all" IsChecked="True" FontWeight="SemiBold" Checked="ChkAll_Checked" Unchecked="ChkAll_Unchecked"/>
                    <Rectangle Fill="{StaticResource VvBorderBrush}" Height="1" Margin="0,8"/>
                    <CheckBox Name="chkColumns" Content="Structural Columns" IsChecked="True" Checked="ChkItem_Changed" Unchecked="ChkItem_Changed"/>
                    <CheckBox Name="chkFraming" Content="Structural Framing" IsChecked="True" Checked="ChkItem_Changed" Unchecked="ChkItem_Changed"/>
                    <CheckBox Name="chkFloors" Content="Floors" IsChecked="True" Checked="ChkItem_Changed" Unchecked="ChkItem_Changed"/>
                    <CheckBox Name="chkWalls" Content="Walls" IsChecked="True" Checked="ChkItem_Changed" Unchecked="ChkItem_Changed"/>
                    <CheckBox Name="chkFoundation" Content="Structural Foundations" IsChecked="True" Checked="ChkItem_Changed" Unchecked="ChkItem_Changed"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Background="{StaticResource VvSurfaceBrush}" BorderBrush="{StaticResource VvBorderBrush}" BorderThickness="1" CornerRadius="4" Padding="16">
                <StackPanel>
                    <TextBlock Style="{StaticResource VvSectionTitleStyle}" Text="PROCESSING METHOD" Margin="0,0,0,12"/>
                    <RadioButton Name="optDataOnly" Content="Calculate volume and assign BIM_Volume" IsChecked="True"/>
                    <RadioButton Name="optParts" Content="Create Parts and assign parameters (recommended for 4D)"/>
                    <RadioButton Name="optPhysical" Content="Physical split by copy and adjust (walls and framing only)"/>
                </StackPanel>
            </Border>
        </Grid>

        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="160"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="120"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Ready to process selected categories." Style="{StaticResource VvSecondaryTextStyle}" VerticalAlignment="Center"/>
            <Button Grid.Column="1" Style="{StaticResource VvPrimaryButtonStyle}" Content="PROCESS ZONES" Click="BtnProcess_Click"/>
            <Button Grid.Column="3" Content="CANCEL" Click="BtnCancel_Click" Style="{StaticResource VvSecondaryButtonStyle}"/>
        </Grid>
    </Grid>
</Window>

