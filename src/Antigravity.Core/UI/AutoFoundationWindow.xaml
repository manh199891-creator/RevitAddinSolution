<Window x:Class="Antigravity.Core.UI.AutoFoundationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="VILAI VIET Auto Foundation Creator" Height="450" Width="400"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        MouseLeftButtonDown="Window_MouseLeftButtonDown"
        WindowStartupLocation="CenterScreen">
    
    <Window.Resources>
        <Style TargetType="Button" x:Key="FlatButton">
            <Setter Property="Background" Value="#1E88E5"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1565C0"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <Style TargetType="Button" x:Key="CloseButton" BasedOn="{StaticResource FlatButton}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E53935"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border Background="White" CornerRadius="10" BorderBrush="#DDDDDD" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect Color="Black" Opacity="0.3" ShadowDepth="2" BlurRadius="10"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="60"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="60"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" Background="#1E88E5" CornerRadius="10,10,0,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="60"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="40"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock Grid.Column="1" Text="VILAI VIET - Auto Foundation" 
                               Foreground="White" FontWeight="Bold" FontSize="16" 
                               VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    
                    <Button Grid.Column="2" Content="X" Style="{StaticResource CloseButton}" Click="CloseButton_Click" 
                            Width="30" Height="30" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,5,5,0"/>
                </Grid>
            </Border>

            <!-- Body -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="15">
                <StackPanel>
                    <Button Content="Set Coordinate Origin (CAD -- Revit)" Style="{StaticResource FlatButton}" Height="40" Margin="0,0,0,15"/>
                    
                    <Grid Margin="0,0,0,15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        
                        <TextBlock Text="Level:" Grid.Row="0" Grid.Column="0" FontWeight="SemiBold"/>
                        <ComboBox Grid.Row="1" Grid.Column="0" Margin="0,0,5,0" ItemsSource="{Binding Levels}" DisplayMemberPath="Name" SelectedItem="{Binding SelectedLevel}"/>
                        
                        <TextBlock Text="CAD Layer:" Grid.Row="0" Grid.Column="1" FontWeight="SemiBold"/>
                        <TextBox Grid.Row="1" Grid.Column="1" Margin="5,0,0,0" Text="{Binding CadLayer, UpdateSourceTrigger=PropertyChanged}"/>
                    </Grid>
                    
                    <GroupBox Header="Foundation Settings" Margin="0,0,0,15" Padding="10">
                        <StackPanel>
                            <TextBlock Text="Family Type:" FontWeight="SemiBold"/>
                            <ComboBox Margin="0,5,0,10" ItemsSource="{Binding FoundationFamilies}" DisplayMemberPath="Name" SelectedItem="{Binding SelectedFamily}"/>
                        </StackPanel>
                    </GroupBox>
                </StackPanel>
            </ScrollViewer>

            <!-- Footer -->
            <Border Grid.Row="2" BorderBrush="#EEEEEE" BorderThickness="0,1,0,0" Padding="10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <Button Grid.Column="0" Content="Draw Foundation" Style="{StaticResource FlatButton}" Margin="2,2,5,2" Click="DrawFoundation_Click"/>
                    <Button Grid.Column="1" Content="Cancel" Style="{StaticResource FlatButton}" Margin="5,2,2,2" Background="#9E9E9E" Click="Cancel_Click"/>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
