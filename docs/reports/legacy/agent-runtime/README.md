# Retired Agent Runtime Archive

Status: HISTORICAL / NOT ROUTABLE

This directory preserves migration and audit evidence from the retired project-local `dual-agent` and `dual-agent-pipeline` capabilities.

These files are historical evidence only. They are not active project instructions, are not skill-discovery candidates, and must not be used to start research, planning, implementation, review, repair, release, scheduling, retry, or recovery flows.

Current authority is defined by `.agents/AGENTS.md`, `.agents/policies/capability-routing.md`, `.agents/policies/consolidation-governance.md`, active skill entries in `.agents/skill-manifest.json`, and the Local Orchestrator runtime.

Where archived text mentions retired capabilities such as `1-spec`, `2-plan`, `3-code`, `4-ship`, `dual-agent`, or `dual-agent-pipeline`, treat those references as historical snapshots of the pre-consolidation architecture, not current routing guidance.
