# PLAN.md - mock_task

## Goal
Implement a mock feature to test the artifact contract.

## Implementation Steps
1. Create `src/Antigravity.Core/MockComponent.cs` for logic.
2. Update `tests/Antigravity.Core.Tests/MockComponentTests.cs`.

## Test Strategy
1. Build the solution.
2. Run pytest suite.

## Rollback Procedure
If placement fails, revert the commit.
